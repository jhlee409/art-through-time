# Art Through Time 작업 규칙

## 작업 전 작성규칙 확인

- 코드, JSON, HTML, 이미지, 문서를 수정하기 전에는 `README.md`의 `작업 전 필수 작성규칙` 섹션을 먼저 확인한다.
- 모든 탭·전용 화면 작업은 `작성규칙/새탭_template_작성규칙.md`와 작업 대상에 해당하는 탭별 작성규칙을 함께 적용한다. 새 탭을 만들 때는 먼저 새 탭 템플릿의 기본 구성과 인증·동기화·검증 기준을 구현한 뒤 세부 기능을 만든다. 예를 들어 사조 설명 HTML은 `작성규칙/사조설명html_작성규칙.md`, 화가 및 작품 탭은 `작성규칙/화가및작품탭_작성규칙.md`, 화가 해설은 `작성규칙/화가설명_작성규칙.md`를 반드시 확인한다.
- 여러 탭이나 정본 데이터가 함께 바뀌는 작업은 관련 작성규칙을 모두 확인하고, 완료 보고에 참고한 작성규칙과 실행한 검증을 남긴다.

## 사조 HTML 작성규칙 동기화

- `data/미술사조/*.html`을 수정할 때는 변경에 새롭거나 바뀐 재사용 규칙이 있는지 확인한다.
- 새 규칙·예외·레이아웃 표준·검증 절차가 생기거나 달라졌다면 같은 작업에서 `작성규칙/사조설명html_작성규칙.md`에 추가하거나 기존 항목을 정리한다.
- 일회성 작품명·화가명·문장 교정은 작성규칙에 넣지 않는다. 다른 사조 문서에도 적용할 수 있는 기준만 기록한다.
- 사조 HTML을 수정했지만 작성규칙 변경이 필요 없으면, 기존 규칙을 그대로 적용한 단순 데이터·문구 변경임을 작업 결과에 명시한다.

## 사조 설명 대표 미술가와 작품 카드 원칙

- 새로 만들거나 전면 재구축하는 사조 설명 문서는 국가·지역을 작가 분류 기준으로 사용하지 않는다. `초심자 핵심` 대표 미술가를 먼저 두고, 이후 `중급 확장` 범주에 미술가를 추가한다. 활동 국가는 작가의 이동과 후원 환경을 설명하는 정보로만 적는다.
- 대표 미술가 표에는 학습 단계, 미술가와 분야, 주요 활동 국가, 미술가 고유의 특징, 사조 특징을 가장 잘 보여 주는 대표 작품과 연도를 적는다. 명백히 성립한 화파·작업실 계보·집단이 있을 때만 별도 화파 표를 둔다.
- 대표작 카드에는 대표 미술가 표에 있는 미술가와 작품을 같은 순서로 모두 포함한다. 표에 없는 미술가·작품을 카드 수를 채우기 위해 넣지 않는다.
- 대표작 카드는 전체 폭 3열 카드 레이아웃(이미지, 미술가명·작품명, 연도·사조 정보, 편집 가능한 선정 이유와 설명)을 유지한다. 화가 목록에 등록된 미술가는 연표 링크를 연결한다.
- 기존 국가 전개형 문서는 해당 사조를 새 구조로 전환하기 전까지 호환 자료로 유지한다. 전환한 문서에는 `data-art-atlas-document-model="artist-guide"`를 기록하고 국가 전개 행·지역 범주 카드 묶음을 남기지 않는다.
- 새로 만들거나 전면 재구축하는 사조 설명 HTML은 반드시 `신고전주의.html`처럼 사람이 식별할 수 있는 사조 이름을 파일명으로 사용하고 `data/미술사조/index.json`에서 그 파일을 직접 연결한다. 교체가 끝난 이전 해시 HTML은 활성 폴더와 색인에서 제거하며, 해시 파일 탐색·리다이렉트·대체 경로 fallback으로 새 문서에 연결하지 않는다.

## 이미지 파일 다운로드 승인 규칙

- URL에서 이미지 파일을 찾아 다운로드하거나, URL 응답을 파일로 저장하는 작업은 반드시 사용자에게 먼저 명시적으로 허락을 받아야 한다.
- 사용자가 "다운로드 폴더"라고 말하면 프로젝트 루트의 `다운로드용/` 폴더만 확인하고, 그 안의 기존 로컬 파일만 사용한다. 파일이 없으면 외부에서 받지 않고 `이미지 업로드 예정` 상태로 남긴다.
- 화가 작품 이미지, 미술사조 이미지, 기법·용어 이미지, 주제 이미지에는 외부 이미지 URL 의존을 새로 만들지 않는다. 로컬 파일이 없으면 사용자에게 파일을 넣어 달라고 말한다.
- Wikipedia, Wikidata, museum pages, 웹 검색은 원어명, 생몰년, 국적, 작품명, 설명, 출처 확인 같은 텍스트 정보 보충에만 사용한다.
- 정말 URL 이미지 파일 다운로드가 필요할 때는 실행 전에 사용자에게 어떤 URL/사이트에서 어떤 파일을 받을지 설명하고 허락을 받은 뒤 진행한다.
- URL 파일 다운로드 스크립트를 새로 만들 경우 `tools/url-download-permission.js`의 `requireUrlFileDownloadApproval()`을 호출해야 하며, 검사 도구 `node tools/check-url-download-approval.js`를 통과해야 한다.
