# 변경사항 2026-08-28


- 화가 리스트 탭을 부모 사조 행과 선택 국가별 세부 사조 하위 상자 구조로 전환했다. 화가 생애 상자는 전체 기간을 유지하고, 하늘색 활동 구간만 해당 하위 사조 기간 안에 표시한다.

- 기본 세로 화면에서 부모 사조 행 약 5개가 보이도록 조정해 국가별 세부 사조 상자와 그 내부 화가 이름 상자의 가독성을 높였다.

- 부모 사조와 세부 사조의 동일 높이 제한을 없앴다. 세부 사조는 공통 높이의 화가 이름 상자 패킹 줄 수에 따라, 부모 사조는 세부 사조 배치에 따라 자동으로 높이를 계산하며, 화가가 없는 부모 사조는 이름 상자 3개 높이를 기본값으로 사용한다.

- 화가 이름 상자를 한 줄 고정으로 유지하고 공통 높이를 기존의 80%로 줄였다. 세부 사조 제목은 상자 내부 대신 상단 테두리 왼쪽에 걸치도록 배치했다.

- 화가 이름 상자를 화가 및 작품 탭 왼쪽 목록과 같은 축약 한글 이름으로 고정하고, 시간상 겹치는 세부 사조 상자의 세로 간격을 넓혔다.

- 세부 사조 제목과 사조 열 제목·보조 정보를 화가 이름 상자와 같은 글꼴 계열·굵기·줄높이로 통일했다.

- 기본 화면의 사조 열과 세부 사조 제목에도 비균등 확대 역보정을 적용해 화가 이름과 같은 정상 가로·세로 비율로 표시되게 했다.

- 세부 사조 제목 글자 크기를 8px에서 12px으로 1.5배 키웠다.

- 세부 사조 안의 화가 이름 상자 줄 간격을 1px로 줄이고, 상단 2px·하단 8px의 비대칭 안쪽 여백을 적용했다.

- 세부 사조 상단 경계와 첫 화가 이름 상자 사이 여백을 2px에서 6px으로 늘렸다.

- 세부 사조가 있는 부모 사조의 기간 막대를 숨기고, 같은 부모의 세부 사조 상자에는 공통 바탕색을 적용했다. 부모 사조별 색은 여섯 색 팔레트를 순환한다.

- 부모 사조 막대 조건부 렌더링에서 세부 사조 목록의 변수 범위를 수정해 화면 전체가 표시되지 않던 오류를 복구했다.

- 화가 리스트의 부모·세부 사조 레이아웃, 가변 높이, 공통 이름 상자, 확대 역보정, 드래그 정렬과 공유 탭 분기를 회귀 점검했다. 국가별 미술·사조·화가 및 작품 탭 경로와 로컬 서버의 화면·스크립트·데이터 응답이 정상임을 확인했다.

- 세기별 가로 폭을 기본 맞춤 폭의 1.3배로 늘려 화면 밖 시간 구간을 가로 스크롤·드래그로 탐색하게 하고, 세기·사조 행 경계선을 더 진하게 표시했다.

- 가로 이동 중 고정되는 첫 사조 셀의 원본 높이를 세로 확대율로 역보정해 오른쪽 세기 밴드와 화면상 38px로 맞추고, 글자 크기를 14px로 통일했다.

- 세기 밴드와 사조 선택 셀에 같은 역보정 높이를 적용해 확대·축소 중 경계와 첫 행 시작선을 정렬했다.
- 화가 이름 상자의 화면상 높이를 약 19.2px로 두 배 확대하고, 세부 사조 상자의 위·아래 안쪽 여백을 각각 8px로 통일했다.

- 사조 선택 고정 열의 폭을 일반 국가별 미술 화면의 60%로 줄이고, 시간축과 고정 경계 계산도 같은 폭을 사용하도록 맞췄다.
- 화가 이름 상자를 중심 기준으로 현재 높이의 1.8배인 화면상 34.56px로 확대하고 자동 패킹 높이를 갱신했다.

- 폭을 줄인 사조 선택 열의 제목·보조 정보 글꼴을 한글 UI에 맞는 굵기, 줄높이, 자간과 색으로 조정했다.
- 화가 이름 상자를 중심 기준으로 다시 1.8배 확대해 화면상 높이를 62.208px로 적용했다.

- 사조 열에서 겹치던 국가·기간 보조 정보 줄을 숨기고 사조명 한 줄만 정상 비율의 글꼴로 표시하도록 정리했다.
- 화가 이름 상자를 중심 기준으로 다시 1.8배 확장해 화면상 약 111.974px 높이로 적용했다.

- 사조 선택 열 제목을 세기 밴드와 같은 700 14px/1 글꼴·크기로 통일했다.
- 이름 상자 표시 축소율을 94%로 완화해 상자 사이 여백을 줄이고, 세부 사조 안쪽 위·아래 여백을 각 12px로 같게 조정했다.
- 시간상 겹치는 세부 사조 상자 사이의 세로 간격을 18px로 확대했다.

## 미술사조 이해 탭

- 오른쪽 위 표시 밀도 슬라이더를 제거하고 사조 비교 영역의 마우스 휠로만 세로 확대값을 조절하도록 변경
- 미술 사조의 이해 탭 전용 휠 확대 범위를 0.5x~5x로 설정하고 가로 폭은 유지하도록 반영
- 탭별 휠 줌 방식은 서로 통일하지 않는다는 기준을 README와 미술사조 이해 탭 작성 규칙에 기록

- 사조 비교 영역의 빈 배경을 마우스 왼쪽 버튼으로 드래그하면 상하좌우로 이동하도록 변경
- 사조 탭 드래그 이동이 국가 제목 정렬·사조 막대·역사 사건 버튼과 충돌하지 않도록 예외 처리

- 사조 비교 영역의 휠 세로 확대·축소 감도를 기존 1.1배 계수에서 1.15배 계수로 높임

- 오른쪽 상단 기간 선택 슬라이더 박스를 제거하고 사조 비교 영역을 전체 기간 고정 표시로 변경해 세로 공간을 확보함

- 사건 라벨 겹침을 줄이기 위해 사조 비교 영역의 세로 휠 확대 상한을 5x에서 7.5x로 높임

- 오른쪽 사조 비교 영역이 화면 아래 끝까지 남은 세로 공간을 모두 쓰도록 패널과 기본 차트 높이 계산을 조정함

- 사건 라벨 겹침 완화를 위해 사조 비교 영역의 세로 휠 확대 상한을 7.5x에서 11.25x로 높임

- 역사 사건 분류에 과학·경제를 추가하고 왼쪽 사건 분류 버튼을 한 줄 4개 배치로 변경함

- 가로 드래그 중 사건 컬럼과 세기 표시 컬럼을 왼쪽에 고정하고 두 컬럼의 가로 폭을 기존 대비 80%로 줄임

- 가로 드래그 중 세기 컬럼 왼쪽으로 사조 막대가 비치지 않도록 고정 사건·세기 컬럼 주변에 배경 마스크를 추가함


- 사조 막대 더블클릭 시 국가별 세부 사조명을 그대로 검색해 Not found로 가던 문제를 수정했다. 저장된 설명 문서가 있으면 직접 열고, 없으면 사조 분류 규칙의 부모 설명 문서를 우선 연다.

- 사조 막대 더블클릭으로 설명 문서를 열 때 모든 문서가 Not found가 되던 서버 오류를 수정했다. 분리된 콘텐츠 서비스에 고해상도 경로 검사 함수를 전달하고, 누락된 HTML 디코딩·태그 속성 유틸리티를 복구했다. 로컬 서버에서 index.json 등록 문서 33개가 모두 HTTP 200으로 열리는 것을 확인했다.

## 미술 사조의 이해

- 과학·경제 사건에 1629~1631년 이탈리아 페스트, 1665~1666년 런던 대페스트, 1720~1722년 마르세유 대페스트를 추가함

- 기간 사건의 아래 연결선을 사조 탭에서만 4px로 두껍게 하고, 위에서부터 검정·빨강·파랑·초록을 반복 적용함

- 사조 탭의 기간 사건 연결선 시작점을 사건 라벨 중앙에서 왼쪽 끝으로 이동함

- 사건 라벨 수평 연결선을 세기 박스 내부가 아닌 왼쪽 여백까지만 표시하도록 조정함

- 사조 탭의 기간 사건 세로 연결선 시작점을 라벨 왼쪽 테두리에서 사건 이름 첫 글자 아래로 이동함

- 사조 탭의 사건 이름을 줄바꿈 없이 한 줄로 표시하고, 긴 이름에 맞춰 사건 컬럼 폭을 조정함

- 사건 이름 컬럼 폭을 가장 긴 라벨과 연결선 여백 기준으로 계산해 내부 왼쪽 여백을 최소화함

- 과학·경제 사건에 1830~1870년 철도 보급과 1840~1880년 사진기 보급을 추가함

- 사조 탭 전용 변경을 점검해 공용 atlas 스크롤·축·사건 스타일을 원래값으로 복구하고 movement-popup 범위로 분리함
- 기간 사건 색 순서가 1950년 이후 공통 구간에서도 이어지도록 보완하고 탭 작성 규칙에 범위·검증 기준을 정리함


- 사조 비교 연표의 시작 범위를 1400년으로 유지하고 후기 비잔틴 미술(1261-1453)과 고딕의 15세기 지속 구간을 시작 경계에서 비교하도록 정리했다.
- 덴마크·노르딕 르네상스는 북방 르네상스의 지역적 전개로, 러시아 바로크·사실주의는 상위 사조의 지역적 전개로 분류하고 별도 설명 문서 등록을 통합했다.
- 북방 르네상스 설명을 저지대·독일어권·프랑스의 공통성과 차이를 비교하는 문서로 개편하고, 국가별 막대에 학술적 유형 표기를 추가했다.
- 사조 문서 분리 기준과 1400년 시작 범위 원칙을 작성규칙에 동기화하고 사조 링크·이미지·국가별 대표작 카드 및 프로젝트 상태를 검증했다.

- 고딕 미술·영국 고딕·북유럽 고딕과 후기 비잔틴 미술을 부모 사조가 아닌 1400년 출발점의 이전 미술 참고 막대로 지정했다.
- 맥락 전용 막대는 사조 비교 탭에서 설명 문서 연결을 유지하되 국가별 미술과 화가 리스트의 정규 사조 행에서는 제외하도록 contextOnlyMovements 규칙을 추가했다.

## 프로젝트 점검

- 사조 HTML 14개에 남아 있던 base64 인라인 이미지 45개를 `data/미술사조/images/` 로컬 파일로 분리하고 문서 기준 상대 경로로 교체함
- 사조 HTML 검증에서 `data:image` 인라인 이미지와 누락된 로컬 이미지 경로를 실패로 잡도록 `tools/validate-movement-links.js`를 보강함
- 사조 HTML 작성 규칙에 base64 인라인 이미지를 금지하고 로컬 이미지 파일로 분리한다는 기준을 추가함
- 10MB를 초과하던 티에폴로·카리에라 썸네일 2개를 PNG 표시용 파일로 변환하고 작품 데이터 참조를 갱신함

## 공통 탭 구조

- 모든 왼쪽 사이드바에 우상단 패널형 아이콘 토글을 추가해 사이드바를 감추고 본문을 남은 폭 전체로 확장할 수 있게 함
- 접힌 상태에서는 화면 좌상단에 복원 토글만 남기고, 사이드바 표시 상태를 각 브라우저 탭에서 독립적으로 유지함
- 공통 및 각 탭 작성 규칙에 사이드바 보이기·감추기 토글과 탭별 독립 상태 기준을 추가함

## 프런트엔드 코드 구조

- `app.js`를 공통 상태·데이터 처리, 화가·작품 화면, 사조·국가별 화면, 상세 보기, 초기화 파일로 분리하고 기존 실행 순서를 유지함
- 서버의 정적 파일 허용 목록에 분리한 앱 스크립트를 추가함

## 서버 코드 구조

- `server.js`에서 외부 데이터 조회·화가 저장·썸네일 처리를 `server-data.js`로, 사조 문서·이미지 업로드·정적 파일 처리를 `server-content.js`로 분리함
- 서버 진입점에는 인증, 규칙 점검, 라우팅만 남기고 기존 API 경로와 인증 순서를 유지함
- 규칙 점검이 분리된 앱·콘텐츠 모듈의 사조 분류 기준을 확인하도록 갱신함

## 로그인 진입

- `?login=1`로 연 화면에서는 탭 세션뿐 아니라 공유 관리자 세션도 지워, 실행 파일을 다시 열 때 로그인 화면을 건너뛰지 않게 함
- 공용 새 탭의 첫 데이터 렌더링이 끝날 때까지 기본 화가 사이드바와 탭 이동을 숨겨 다른 탭에서의 화면 깜빡임을 제거함

## 분할 후 전수 점검

- 화가 목록 국가 아이콘·사조 필터·사조 분류 규칙 검사가 더 이상 축소된 `app.js`만 읽지 않고 분리된 전체 앱 모듈을 확인하도록 수정함
- 규칙 점검 보고서가 실제 코드 소유 파일인 `app-core.js`, `app-artists.js`, `server-content.js`를 가리키도록 정정함
- 공용 탭 작성 규칙에 앱·서버 모듈 책임, 로딩 순서, 분할 뒤 규칙 검사 대상 갱신 기준을 기록함
- `npm test`의 프로젝트 건강검사에 앱 스크립트 로딩 순서, 정적 파일 허용 목록, 서버 규칙 검사의 전체 앱 모듈 대상 여부를 추가함

## 성능·데이터 구조

- 화가 목록 초기 로딩을 data/artists-index.json 인덱스로 분리하고, 화가 선택 시 /api/artists/:id 상세 데이터를 불러오도록 변경함
- 서버 저장 시 아직 상세를 열지 않은 화가의 작품 목록을 기존 원본에서 보존하고, 저장 뒤 인덱스를 자동 재생성하도록 처리함

- 화가·화가 리스트 화면이 전체 작품 데이터에 의존하는 기존 기능을 확인해, 초기 지연 로딩 전환은 보류하고 전체 데이터 로딩으로 복구함. artists-index.json과 상세 API는 향후 단계적 전환용으로 유지함

## 화가 리스트 탭

- 기본 선택 국가를 네덜란드·덴마크·독일·벨기에·스페인·영국·오스트리아·이탈리아·프랑스로 바꾸고, 기존 저장 선택과 구분하는 v2 기본값을 적용함
- 가로 시간축 기본 맞춤 폭은 유지한 채, 기본 세로 화면에서 사조 행 약 18개가 보이도록 화가 리스트 전용 세로 배율을 조정함

- 기본 선택 국가에 이미 있던 독일은 유지하고 러시아를 추가해, 10개국의 동시대 사조·화가 비교 화면으로 구성함

- 기본 선택 국가에 스위스를 추가해 취리히 다다를 포함한 국제 전위미술 교류를 기본 비교 범위에 반영함

- 기본 세로 화면에 보이는 사조 행 수를 약 18개에서 약 12개로 줄이고, 나머지는 세로 스크롤로 보도록 조정함

- 사조 고정 열 글자의 비균등 확대 보정을 실제 화면 배율 기준으로 바꾸고 제목·보조 글자 크기를 낮춰 일반적인 글자 비율로 보이게 함
- 모든 화가 이름 상자의 공통 높이와 글꼴을 중심 기준으로 78%로 줄여, 생애 위치는 유지하면서 상자 사이 여백을 확보함

- 화가 이름 상자 안에서 activeFrom부터 등록 작품의 가장 늦은 제작 연도까지를 연한 하늘색으로 표시해 독립적 활동 시작과 마지막 등록 작품 시기를 구분함

- 세부 사조 제목을 더블클릭하면 부모 사조 설명 문서의 여러 국가에서의 전개 표에서 해당 국가·지역의 특징만 읽기 전용 창에 표시하도록 추가했다. 같은 제목을 다시 더블클릭하면 닫히며, 창 제목줄 드래그 이동과 네 변·모서리 드래그 크기 조절을 지원한다.

- 세부 사조 지역 특징 보기 창의 닫기 동작을 제목 재더블클릭이 아니라 열린 창 자체 더블클릭으로 변경했다.

- 지역 특징 보기 창의 열기 동작을 세부 사조 제목에서 세부 사조 박스 내부의 화가 이름 상자·제목 외 빈 공간 더블클릭으로 옮겼다. 창 자체 더블클릭 닫기는 유지했다.

- 지역 특징 보기 창의 기본 가로 폭을 제목과 닫기 안내가 한 줄로 보이도록 자동 계산하고, 기본 세로 높이를 본문 약 10줄을 읽을 수 있게 조정했다.

- 세부 사조 박스의 지역 특징 창이 열릴 때마다 부모 사조 설명 HTML을 새로 읽도록 캐시를 제거했다. 특징 셀의 번호·불릿 원본 형식은 유지하고 편집 아이콘·입력·저장·취소 제어는 제외하며, 저장 전 편집 중인 내용 대신 마지막 저장 내용을 표시한다.

## 공통 화면

- 모든 탭 오른쪽 위의 전체 규칙 점검 버튼과 탭별 규칙 점검 클라이언트 참조를 제거했다. 서버의 규칙 점검·보고서 생성 기능은 유지하며 필요 시 개발·점검 절차에서 실행한다.

## 바로크 사조 설명

- 바로크의 여러 국가에서의 전개 표에 국가별 특징 전용 관리자 편집기를 추가했다. 번호 제목·불릿 목록 형식, 40줄 제한, 저장·취소, 마지막 내용 뒤 인라인 편집 아이콘, 왼쪽·세로 가운데 정렬을 적용했다.

- 바로크 국가별 특징의 편집 아이콘이 권한 상태와 무관하게 사조 설명 팝업에 표시되도록 수정했다. 관리자는 편집·저장, 보기 권한 사용자는 관리자 로그인 안내를 받는다.

- 여러 국가에서의 전개 구역을 문서 가로 전체로 확장하고 국가·지역·대표 화가 열을 한 줄 최소 폭으로 조정했다. 국가별 특징 편집 아이콘은 각 특징 칸의 오른쪽 위로 옮겼다.

- 여러 국가에서의 전개 표의 국가·지역·특징·대표 화가 컬럼 사이를 흰색 1px 실선으로 구분했다.

## 사조 설명·화가 리스트 동기화 규칙

- 사조 설명의 여러 국가에서의 전개 표에서 국가·지역·세부 사조·기간·대표 화가를 변경할 때 data/art-movements.json, data/artists.json, 화가 리스트 세부 사조 상자·화가 이름 상자를 같은 작업에서 반드시 동기화·검증하도록 사조 HTML 규칙, 화가 리스트 규칙, README에 명시했다.

## 화가 리스트 정본 규칙

- 화가 리스트 오른쪽 화면의 정본을 각 부모 사조 설명 HTML의 여러 국가에서의 전개 표에 있는 국가별 세부 사조와 대표 화가로 명시했다. art-movements.json과 artists.json은 해당 표를 화면에 배치하는 동기화 데이터로 규정하고, 사조 HTML 규칙·화가 리스트 규칙·README를 갱신했다.

## 전체 사조 설명 문서

- 33개 사조 설명 HTML의 여러 국가에서의 전개를 국가·지역·세부 사조 / 지역적 특징 / 대표 화가·제작자 3컬럼 표로 통일했다. 특징은 번호 제목·불릿 목록 형식, 표 전체폭, 국가·대표 열 한 줄 폭, 흰색 컬럼 경계, 공통 편집 식별자를 적용했다. 표가 없던 비더마이어·도나우파·러시아 이콘화 문서는 근거를 보존한 3컬럼 표로 전환했으며, 전체 정규화 도구와 절차를 작성 규칙에 기록했다.

## 프로젝트 점검 및 정리

- 삭제된 전체 규칙 점검 버튼의 클라이언트 CSS·JS와 정적 파일 허용 목록을 제거해 이전 UI 잔여물을 정리했다.
- 국가별 전개 표 편집 규칙을 country-development 식별자로 통일하고 바로크 전용 중복 스타일·런타임 주입 코드를 제거했다.
- 전체 데이터·사조 링크·표 정규화·URL 다운로드 승인·JS 문법 및 33개 사조 문서 HTTP 응답을 재검증했다.

## 문서·검증 절차 정비

- README의 전체 규칙 점검 절차에 프로젝트 건강검사, 국가별 전개 표 정규화 검사, 사조 문서 HTTP 응답 확인 기준을 보완했다.
- 공통 탭·사조 HTML·화가 리스트 작성규칙에 미사용 정적 파일 제거, 공통 표 식별자, 관련 문법·데이터 검증 절차를 명시했다.

## 읽기 전용 권한

- 읽기 전용 사조 설명에서는 여러 국가에서의 전개 표의 지역 특징 편집 아이콘도 렌더링하지 않도록 수정했다.

## 로코코 국가 전개·심화 카드 동기화

- 로코코 프랑스 대표 화가 표에 심화 대표작 카드 순서대로 샤르뎅을 복구했다.
- 심화 대표작 카드를 저장할 때 같은 국가·세부 전개 그리드의 화가 전체와 순서를 국가 전개 표의 같은 행에 자동 동기화하도록 보완했다.

## 국가 전개 표·심화 카드·화가 리스트 정본 동기화

- 모든 사조 설명 문서의 국가 전개 표 링크를 화가 리스트가 직접 읽어 해당 세부 사조 상자에 표시할 화가를 제한하도록 변경했다.
- 심화 카드 저장 시 같은 국가·세부 전개 카드의 화가·순서를 표에 자동 반영하고, 카드가 아직 없는 표 화가는 삭제하지 않도록 보호했다.
- 사조 설명 저장 신호를 받으면 화가 리스트가 표 정본 캐시를 비우고 최신 대표 화가로 다시 그리도록 연결했다.

## 화가 리스트 국가 전개 표 기준 전환

- 화가 리스트의 세부 사조 상자를 사조 탭의 전체 국가별 사조 데이터가 아니라 각 사조 설명 HTML의 국가 전개 표 행에서 생성하도록 전환했다.
- 사조 데이터는 표에 정한 세부 사조 상자의 기간 좌표만 제공하며, 표의 세부 사조명·대표 화가·순서만 표시하도록 했다.
- 로코코처럼 사조 탭의 넓은 연표 범위와 국가 전개 표의 선별 범위가 다른 경우에도 화가 리스트는 후자를 따른다는 기준을 README와 탭 작성규칙에 기록했다.

## 화가 리스트 이름 상자 형식 복원

- 국가 전개 표 행 기반 전환 뒤에도 이름 상자의 화면상 높이를 원본 12px의 80%인 9.6px로 고정하고, 세부 사조 상자의 상단 6px·이름 상자 간 1px·하단 8px 간격을 유지하도록 복원했다.

## 사조 전면 재구축

- 재구축 전 현재 구조를 반복 점검하는 tools/audit-movement-rebuild.js를 추가했다.
- 44개 부모 분류, 등록·미등록 HTML, 국가 전개 표, 대표 화가 링크, 마지막 심화 대표작 카드와 범주 연결 상태를 data/미술사조/REBUILD_BASELINE.json 및 REBUILD_BASELINE.md에 기준선으로 기록했다.
- 이번 단계에서는 사조 HTML을 수정하지 않았으며 기존 작성규칙을 그대로 적용했다.

## 사조 전면 재구축 2단계

- data/art-movement-canonical.json에 부모 사조 44개, 독립 문서 34개, 흡수형 10개, 초심자 핵심 범주 68개의 안정 ID와 역사적 표시 순서를 확정했다.
- 독립 문서를 상세형 20개·연결형 6개·참고형 8개, 표시 구조를 세부 범주 연동형 24개·단일 부모형 10개로 고정했다.
- tools/validate-movement-canonical.js와 자동 생성 문서 data/미술사조/CANONICAL_TAXONOMY.md를 추가하고 전체 건강 검사에 정본 검증을 포함했다.
- 정본 ID·흡수형 부모·coveredTopics 사용 기준을 미술사조 이해 탭 및 사조 HTML 작성규칙에 반영했다. 이번 단계에서는 기존 사조 HTML 내용과 DOM 구조를 수정하지 않았다.

## 사조 전면 재구축 3단계

- data/art-movement-sync-contract.json에 parentId·categoryId·developmentId·countryId·artistId·workId의 역할과 버전 1 DOM 속성을 확정했다.
- 국가 전개 표는 국가·범주·특징·대표 화가 구성, 대표작 카드는 작품·이미지·선정 이유·설명, 정본 JSON은 이름·순서를 책임지도록 데이터 권한을 분리했다.
- 같은 developmentId 안의 카드 드래그만 허용하고 표의 화가 순서를 같은 트랜잭션에서 갱신하며, 범주 이동·추가·제거는 표 편집 명령으로 카드와 함께 처리하도록 고정했다.
- tools/validate-movement-sync-contract.js와 자동 생성 문서 data/미술사조/SYNC_CONTRACT.md를 추가하고 프로젝트 건강 검사 및 작성규칙에 포함했다.
- 기존 29개 등록 HTML은 무버전 레거시로 유지했으며 버전 1 준비도 0/29를 4단계 이관 기준선으로 확인했다.

## 사조 전면 재구축 4단계

- 정본 색인을 부모 사조 문서 34개와 후기 비잔틴·고딕 참고 문서 2개, 총 36개로 이관했다. 기존 문서 21개는 본문과 로컬 이미지를 보존했고, 없던 부모 사조 문서 15개는 국가 전개 표와 대표작 카드 묶음을 갖춘 골격으로 생성했다.
- 24개 세부 범주 연동형·10개 단일 부모형의 국가 전개 행과 대표작 카드 묶음에 68개 정본 categoryId와 프로젝트 고유 developmentId를 부여했다.
- 5단계 대표 화가·작품 선별 전에는 편집 연동을 잠그는 `syncState=structure` 상태를 동기화 계약과 작성규칙에 추가했다. 이전 미술 참고 문서는 parentId 대신 contextId로 구분한다.
- `structure` 문서는 관리자 화면에서도 설명 편집·카드 드래그·자동 링크 새로고침·API 저장이 실행되지 않도록 클라이언트와 서버를 함께 잠갔다.
- 정본에서 흡수되거나 하위 자료가 된 기존 문서 8개는 삭제하지 않고 `data/미술사조/legacy-index.json`에 소유 부모·범주·용도를 기록했다.
- 멕시코 벽화운동의 국가 전개를 잘못된 대체 국가에 넣지 않도록 미술 사조 데이터와 국가 선택·별칭에 멕시코를 추가했다.
- 반복 가능한 이관기 `tools/migrate-movement-documents-v1.js`, 이관표 `data/art-movement-document-migration.json`, 검증기 `tools/validate-movement-documents-v1.js`, 자동 보고서 `data/미술사조/HTML_MIGRATION.md`를 추가하고 프로젝트 건강 검사에 포함했다.

## 사조 전면 재구축 5단계

- 초심자가 68개 핵심 범주의 차이를 먼저 잡도록 범주마다 기준점 화가 1명과 대표작 1점, 핵심 특징, 선정 이유, 작품 설명을 `data/art-movement-representatives.json`에 정본화했다.
- 34개 부모 사조 문서의 국가 전개 표를 68개 정본 범주 순서로 정리하고, 표의 대표 화가와 심화 대표작 카드의 artistId·workId·순서를 1:1로 맞췄다. 정본 밖의 기존 표 행과 잔여 대표작 카드 묶음은 연동 화면에서 제거했다.
- 기존 화가 78명 중 재사용 가능한 기록을 유지하고 부족한 기준점 화가 46명을 보충해 `artists.json`과 경량 `artists-index.json`을 124명으로 동기화했다.
- 외부 이미지를 새로 받지 않고 기존 로컬 도판만 연결했다. 읽을 수 있는 도판이 없는 카드는 `imageState=pending`과 `이미지 업로드 예정` 상태로 남겼다.
- 5단계 콘텐츠는 `syncState=content`로 두고 6단계 ID 기반 카드 드래그·표·화가 목록 양방향 동기화 전까지 편집과 저장을 잠그도록 계약, 클라이언트, 서버, 작성규칙을 함께 갱신했다.
- 반복 가능한 재구축기 `tools/rebuild-movement-representatives.js`, 불변식 검증기 `tools/validate-movement-representatives.js`, HTTP 확인기 `tools/check-movement-http.js`, 결과 보고서 `data/미술사조/REPRESENTATIVE_CONTENT.md`를 추가했다.
- 프로젝트 건강 검사에서 OneDrive 클라우드 자리표시자 이미지의 접근 거부를 별도 수치로 기록하고 나머지 검사를 계속하도록 보완했다.

## 사조 전면 재구축 6단계

- 34개 부모 사조 문서를 `syncState=complete`로 전환하고, 이전 미술 참고 2개 문서는 편집 대상이 아닌 `structure` 상태로 유지했다.
- 공통 `movement-sync-v1.js` 파서·검증기를 추가해 현재 파일과 제출 DOM의 parent·category·development·country·artist·work ID를 비교하고, 허용되지 않은 소속·구조 변경을 저장 전에 거부하도록 했다.
- 같은 `developmentId` 안의 카드 순서만 표 대표 화가 링크 순서에 반영하고, 전체 hard 불변식을 통과한 HTML을 임시 파일에서 원자적으로 교체하도록 서버 저장 경로를 바꿨다.
- 카드 편집기는 `ready`와 `pending` 카드 모두에서 선정 이유와 작품 설명을 함께 편집한다. 저장 실패 시 카드·표 순서를 되돌리고, 성공 시 현재 창과 다른 탭의 국가별 미술·화가 리스트 캐시를 모두 갱신한다.
- `art-movements.json` 막대에 canonical parent·document owner·category·development ID를 기록했다. 기존 지도에서 범주 도달 경로가 없던 국가 전개 막대를 보완해 68개 범주와 68개 development ID가 모두 화가 리스트에 연결되도록 했다.
- 화가 리스트와 국가별 대표작은 국가명·활동 지역 문자열 대신 HTML의 `countryIds`·`developmentId`를 우선 사용한다. 표 정본 화가는 보조 국적·사조 메타데이터 불일치만으로 화면에서 제외하지 않는다.
- 완료 스크립트 `tools/complete-movement-sync-v1.js`, 런타임 검증기 `tools/validate-movement-sync-v1-runtime.js`, 결과 보고서 `data/미술사조/SYNC_COMPLETE.md`를 추가하고 전체 건강 검사에 포함했다.

## 전체 프로젝트 점검

- 전체 JavaScript·JSON·데이터 계약·사조 문서 검증 135개와 등록 사조 HTML 36개의 HTTP 응답을 다시 확인했다.
- 관리자 화면을 여는 것만으로 `featured-works` 오버레이와 레거시 출처 보정 결과가 `artists.json`에 자동 저장되던 동작을 제거했다. 페이지 조회는 더 이상 관리자 데이터 파일을 수정하지 않는다.
- 화가 데이터 저장 시 모든 화가와 작품의 `updatedAt`을 일괄 갱신하지 않고 실제로 바뀌거나 작품이 삭제된 화가·작품만 갱신하도록 저장 계층을 수정했다.
- 컬렉션·화가·작품 메타데이터를 정규화할 때 `representativeContentSchema` 같은 사용자 정의 필드를 보존하도록 변경하고, 초기 로딩 자동 저장 금지와 메타데이터 보존 검사를 프로젝트 건강 검사에 추가했다.
- 작품의 사조명이 비어 있을 때 공백 문자열이 모든 화가 사조와 일치한다고 판정되던 오류를 고쳐, 모네의 《인상, 해돋이》 같은 실제 핵심 작품이 사조 기여작 선정에서 밀려나지 않게 했다.
- 한·영 사조명을 언어별로 비교하고 띄어쓰기·하이픈 차이는 허용하되 `후기·신·Post·Neo` 접두어가 만드는 별개 사조를 단순 포함 관계로 오인하지 않도록 사조 일치 판정을 보완했다.
- 화면의 자동 점수 계산이 사람이 고른 대표작을 덮어쓰지 않게 했다. 68개 정본 대표작에는 `canonical-movement-representative` 근거를 부여하고, 해당 화가에게 남아 있던 과거 자동 선택 22건을 해제했으며 검증기에 정본 대표작 연결 검사를 추가했다.
- 대표 사조 카드 68개 중 로컬 이미지가 연결된 카드는 37개, 업로드 대기 카드는 31개이며 외부 이미지 URL과 누락된 ready 이미지 파일은 없음을 확인했다.

## 사조 문서 전수 학습 체계 보강

- 정본 부모 사조 34개와 후기 비잔틴·고딕 참고 문서 2개를 전수 점검하고, 모든 문서의 `<main>` 첫 부분에 같은 구조의 `초심자 학습 길잡이`를 추가했다.
- 36개 사조마다 무엇이 이전과 달라졌는지, 작품에서 볼 세 기준, 인접 사조와의 경계, 흔한 오해를 개별 문장으로 작성했다. 기존 상세 본문과 68개 국가 전개 행·대표작 카드는 보존했다.
- 부모 문서의 핵심 범주 비교는 정본 범주명과 68개 대표 콘텐츠의 핵심 특징을 직접 읽어 구성하도록 해 문서별 중복 문구를 만들지 않았다.
- 길잡이 원문 `data/art-movement-learning-guides.json`과 반복 가능한 동기화·검사 도구 `tools/sync-movement-learning-guides.js`를 추가하고 전체 건강검사에 포함했다.
- 활성 색인 밖의 물리 HTML 12개도 전수 확인했다. 기존 레거시 색인에서 빠진 르네상스 중복본 2개, 사실주의 중복본 1개, 러시아 바로크 맥락 자료 1개를 레거시 문서로 등록해 36개 활성 문서와 명확히 구분했다.
- 사조 HTML 작성규칙에 상세형·연결형·참고형·이전 미술 참고 문서의 학습 목적, 필수 내용, 데이터 소유권과 검증 절차를 기록했다. 외부 이미지 URL이나 이미지 파일 다운로드는 추가하지 않았다.
